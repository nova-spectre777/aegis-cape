# Contributing
Useful contributions include new state-diff invariants, simulator adapters, adversarial-world generators, reproducible false-negative cases, counterexample minimization, and formalized policy subsets. Tests must use synthetic or explicitly authorized data. Never add seed/private-key collection, wallet-draining, credential extraction, or unauthorized signing functionality.
