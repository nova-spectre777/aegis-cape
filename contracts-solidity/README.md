# Solidity research track
Source-only in v0.1 because `solc`/Foundry were not available in the release environment. `OutcomeGuard.sol` is a non-custodial commitment registry; it does not sign transactions, store keys, or transfer assets.
